/**
 * Default settings for FontSizePicker
 */

// Sizes
export const fontSizes = [
    { shortName: 'S', size: 12 },
    { shortName: 'M', size: 16 },
    { shortName: 'L', size: 20 },
    { shortName: 'XL', size: 24 }
];

// Fallback size
export const fallbackFontSize = 14;
